# Self-review, 2026-08-02

Two subagent reviewers were dispatched; both went idle without delivering findings through the
normal channel, so this began as a direct self-review. **The full reviewer report was later
recovered by Kiri from the CLI and is the source of items 7–13 below** — it is far more thorough
than the self-review, and its findings on posted-key misattribution, wake-forged confirmations,
and the unauthenticated socket are the most valuable output of this session.

Where the report and a measurement disagreed, the measurement won, in both directions.

## Fixed

### 0. Two reviewer claims that did not reproduce

The report asserted `"abc".contains("")` is **false** in Swift and that this Mac's
`HIDIdleTime` "never climbs past ~2.6 s". Both were tested directly: `contains("")` returns
**true**, and a control run with nothing posted showed idle climbing normally
(9 → 12 → 15 → 18 → 21 s). Their idle sampling was almost certainly taken while Kiri was using
the machine, which also undermines the control run their disproof (below) depended on.

### 1. The engine's own input made it believe a human was present (critical — re-confirmed)

The reviewer called this disproven, on the grounds that `CGEventSource.counterForEventType`
did not move for posted events. That measurement is real but measures a *different mechanism*:
the HID event counters are not `IOHIDSystem`'s idle timer. Re-tested under control after their
report arrived — idle 39 s, +8 s of nothing → 47 s, one `postToPid` → **627 ms**, then climbing
again 4 s later. Reproducible and unambiguous.

**Measured**: with the machine untouched for 63 s, a single posted keystroke dropped
`HIDIdleTime` to 658 ms. `UserPresence` reads that timer, so the engine would type, observe
"someone is at the keyboard", and report `present` — for as long as it kept working. An agent
running alone overnight would have believed a human was there the entire time, and the
`mayTakeCursor` signal derived from it would have been wrong in the direction that silently
disables the tool.

Worse than a wrong reading: the system was **manufacturing the evidence it used to judge
itself**. Fixed by `InputAttribution`, which records every synthesis and discounts a
last-input moment that coincides with ours.

Two useful details fell out of the measurement: accessibility writes do **not** reset the idle
timer (raw idle stayed at 105 s across an AX write), so only rung 2 and beyond are affected;
and the attribution window is deliberately tight (1.5 s), because over-attributing would hide
a real person, which is the dangerous direction.

### 2. Cursor and focus warnings fired on the user's own activity (high)

The invariance check compared cursor position before and after an action, which cannot
distinguish our movement from the user's hand. A live test flagged "the cursor was moved" for
an accessibility write, which is physically incapable of moving it — Kiri was using the mouse.

A warning that fires on unrelated activity trains people to ignore it, which would destroy the
one guarantee this tool exists to make. Only `hardwareInput` can move the pointer, so movement
under any other rung is the user's. Focus is subtler — an action genuinely can raise its
target — so the test there is whether the app *we were driving* came forward.

### 3. An empty probe confirmed itself (medium — verified against the reviewer's objection)

`readback.contains("")` is always true (measured), so clearing a field reported `confirmed`
regardless of what happened. Empty text is now verified by emptiness. The reviewer believed the
opposite semantics, which would have made the same code fail in the other direction; either way
the explicit empty-string branch is correct.

### 4. A failed attach stranded the virtual display (medium)

`startedByUs` was set *after* `launch()` returned, but `launch()` throws on attach timeout
after the app has already started — leaving a running virtual display that nothing believed it
owned, immune to cleanup. Ownership is now claimed before the wait, with a release on the error
path.

### 5. `virtualScreen` picked the user's external monitor (medium)

"First screen that is not main" selects a real second display when one is attached, which would
park agent windows on a screen the user is looking at — the exact opposite of the intent. Now
matched by name.

### 6. A stuck request would deadlock every future one (medium)

The accept loop is single-threaded and `handle` blocked on an unbounded semaphore. One wedged
request — an unresponsive target can hold an AX call for seconds — would strand the socket
thread permanently and make the app unreachable. Now bounded at 30 s with an error reply.

### 7. Posted text was confirmed even when it landed somewhere else (critical — reviewer)

Rung 2 read its confirmation from `ElementQuery.focused(pid:)`, the app's current first
responder, while labelling the evidence with the element we aimed at. RESULTS.md already
measured this exact case on Safari's address bar: the click fails to focus it, the text lands in
the page, and the reply would read `confirmed` for the wrong field. This broke the tool's
central promise in the one place it matters. Read-back now comes only from the target, and when
text lands elsewhere the attempt names where it went.

### 8. Waking the display forged click confirmations (critical — reviewer)

`focusBefore` was sampled before rung 0. Since every tree is degenerate while the display
sleeps, the focus signature necessarily differs after waking — and rung 2's click verdict is
"focus changed ⇒ confirmed". Every wake-then-click cycle, which is exactly the unattended
overnight case, reported success regardless of outcome. Focus is now sampled after the wake, and
focus-delta evidence is discarded entirely when a wake occurred.

### 9. Nothing prevented the engine from sending a message (critical — reviewer)

The probe hard-refused to post Return; the app passed text straight through, so a newline in a
Discord or Mail composer submits. `type` now refuses control characters unless `submit: true` is
passed explicitly. Related: absent `text` defaulted to `""`, so a request that merely forgot an
argument would silently clear the field — an unrecoverable write. That now errors.

### 10. A substring match could press the wrong button (high — reviewer)

`locate` took `matches.first` from a substring search, so `click --label delete` pressed
whichever element sorted first among several. Ambiguity now errors and lists the candidates.

### 11. `noEffect` was the one verdict never checked against pixels (medium — reviewer)

The fall-through asserted `noEffect` while holding an unused baseline capture — in the case
where the diff is the only evidence available. It now applies the visual check.

### 12. Latent undefined behavior in the pixel diff (medium — reviewer)

`CGContext` was constructed inside `withUnsafeMutableBytes` and drawn into *after* the closure
returned, past the pointer's guaranteed lifetime. Not observed failing at `-O`, but the failure
mode would be silent: an all-zero buffer means every diff reads 0.0 and every visually-verified
action reports "no effect". Both creation and drawing now happen inside the closure.

### 13. One idle connection stalled the entire control plane (critical — reviewer)

`readRequest` blocked in `read()` with no timeout on the single accept thread, so a client that
connects and sends nothing wedges every future request. Now bounded with `SO_RCVTIMEO`.

### 14. The safety indicator never lit (medium — reviewer)

`isDriving` was declared and never assigned, so the menu bar icon was permanently idle —
defeating the architecture's primary user-facing guarantee. It now reflects the router.

## Known and not yet fixed

### A. Engine work runs on the main actor — FIXED

Resolved by `Core/Engine.swift`. Verified at runtime rather than inferred:
`diag` reports `engineOffMainThread: true`.

The shape the constraint forced: `AXUIElement` is **not** `Sendable` (checked against the SDK
directly rather than assumed), so elements cannot cross an isolation boundary at all. `Engine`
is an actor that accepts only `Sendable` inputs — a pid, a `Locator`, a string — and returns
only `Sendable` output, with elements created, used, and discarded inside. `TreeCache` stopped
being an actor in the process: the engine already serializes it, so a second actor added hops
and bought nothing. `CommandRouter` is now a translator with no accessibility code in it.

Original description follows.

### A′. Engine work ran on the main actor (high — historical)

`CommandRouter` is `@MainActor`, and `GhostLadder.perform` is `nonisolated async`. Under
SE-0461 a nonisolated async function runs on the **caller's** executor, so the whole ladder —
including synchronous AX reads with a 2 s messaging timeout each — executes on the main thread.
`CommandRouter.locate()` is worse: it calls `ElementQuery.named()` directly, a full deep walk
that measured 1.2 s on Discord, straight on the main actor.

Consequence: an unresponsive target app can hang the menu bar UI, and a large walk blocks it for
over a second. The cache does not help here — `TreeCache` is an actor, so walks routed through
it correctly run off-main; `locate()` bypasses it.

The fix is not a one-liner: `AXElement` wraps a non-`Sendable` `AXUIElement`, so moving the work
off the main actor requires deciding what crosses the boundary — most likely resolving elements
to a `Sendable` handle inside a dedicated engine actor and never passing raw elements out.

### B. `TreeCache` blocks its actor for the duration of a walk

The walk closure executes inside the actor, so a 1.2 s walk serializes every other cache user.
Acceptable while requests are handled one at a time (see #6), but it stops being acceptable the
moment the accept loop handles clients concurrently.

### C. No rail against destructive actions

The probe deliberately refused to post Return; the app has no such rule. `type` into a
composer plus a stray Return sends a message; `AXSetValue("")` destroys field contents with no
undo. Worth an explicit policy before this is used unattended — Codex's one enterprise knob
being about the locked screen suggests where the real risk is judged to lie.

### D. No element re-fetch after invalidation

Raised by disassembly of Codex's implementation, which ships a `RefetchableSkyshotAXTree` that
re-finds an *equivalent* element after the original handle goes stale — and whose own strings
concede "multiple equivalents; cannot guarantee uniqueness". Our read-back path holds the
original `AXElement` across the action; Electron in particular rebuilds elements on focus, so a
stale handle can read as nil and turn a successful action into a `noEffect` verdict.
`tryPostedEvents` already re-resolves through the focused element, but `tryAccessibility` and
the pixel-diff frame lookup do not.

Their other technique worth stealing: keyboard shortcuts by `AXPress` on the matching **menu
item**, located via `AXMenuItemCmdVirtualKey`. That sidesteps our measured Electron limitation
entirely — Chromium ignores keycode-only posted events, but it honors a menu item press, so
Cmd+A and friends become reachable without any CGEvent at all.

### E. Force-casts in the AX layer — premise inverted

I recorded these as a crash risk. The reviewer showed the opposite: casting to a CoreFoundation
type performs no runtime check at all, so they cannot trap — the compiler even rejects `as?`
there. The real cost is diagnosability: an app returning `kCFNull` for a focused element yields
an `AXElement` reporting role `"?"` with no frame, and the engine says "exposes no press action"
instead of "this app returned garbage". Fix is a `CFGetTypeID` guard before each cast.

### F. Remaining reviewer findings — now closed

All fixed and verified on the signed build: the socket now verifies the peer's **code
signature** (see below); the rung 1 → rung 2 fall-through no longer retypes over a write that
landed; `mayTakeCursor` is false whenever the screen is locked; sub-threshold pixel deltas stay
`unverifiable` instead of claiming `noEffect`, with the threshold raised past a caret blink;
the diff declines when the target rect moved; `ScreenCapture` no longer falls back to display 0;
`ElementQuery` and `label` stop re-reading attributes they already have; `DisplayWake` reuses
its assertion id and `Hold` is finally instantiated; and `TreeCache` evicts dead processes and
refuses to cache an empty fingerprint.

**The socket is authenticated.** Signing the CLI made this possible: connections must satisfy
`anchor apple generic and certificate leaf[subject.OU] = "52K336H235"`, checked against the
peer's **audit token** rather than its pid, since a pid can be recycled between the check and
the work. Verified three ways — the embedded signed CLI is accepted, an unsigned development
build of the same source is refused, and a raw `nc -U` connection is refused.

Consequence worth knowing: `./RocuroniumCLI/.build/debug/rocuronium` no longer works. That is
correct — the supported client is the one inside the bundle — but it means the debug loop is
now build-sign-install rather than `swift build`.

All three items flagged still-open here were closed by later same-day commits: the cache
fingerprint (`489abaf` — richer fingerprint, re-taken on every hit *and* after the walk, plus a
TTL for changes no fingerprint can see), lease refcounting (`1334f89`), and engine work on the
main actor (item A, fixed by `Core/Engine.swift`).

## Third pass: three reviewers over the day's diff (2026-08-02, evening)

Three subagents reviewed `b93aa30~1..HEAD` under separate lenses — evidence correctness,
concurrency/resource safety, security/destructive actions. This time they all delivered.
Two of them independently found the same closed-menu-item geometry bug from different
directions, which is the clearest signal the lenses were worth separating. Every claim below
was re-verified locally before acting; two reviewer figures were wrong in detail and are
corrected in place.

### 20. Typing an emoji killed the app (critical)

`UniChar` is `UInt16`, so `UniChar(scalar.value)` traps on any scalar above U+FFFF. Verified
by execution: a binary typing `"hi"` completes, `"hi👍"` exits 133 (SIGTRAP). Reachable from
`type --text` with any emoji whenever rung 1 does not confirm — which is *always* on WebKit
and common in Electron. The blast radius is the whole app: the control socket, every lease,
and the virtual-display teardown that a crash never runs. Present in `EventPoster` since the
beginning and faithfully copied into the new `HardwareInput`. Both now post per-scalar UTF-16
payloads, so a non-BMP scalar goes out as its surrogate pair in one event.

### 21. Any app's menu bar reaches Log Out (high)

Measured across the live AX trees: **every** regular app exposes the Apple menu, so
`shortcut --app TextEdit --keys cmd+shift+q` resolves to "Log Out Kirie…" and
`cmd+opt+shift+q` to the variant that logs out with no confirmation dialog. An agent reaching
for a text shortcut could end the session from any target. Item 9 had already established the
principle — `type` refuses a bare newline because a verb that can send must say so before it
acts — and `shortcut` shipped without the equivalent.

Now: items whose resolved path names a session- or data-destroying action (log out, shut down,
restart, sleep, lock screen, empty trash, move to trash, erase) are refused unless
`confirm: true`, and the refusal names both the item and the consequence. `resolveOnly: true`
reports what a shortcut maps to without pressing it, which makes the verb auditable *before*
the fact rather than after. Quit is deliberately not on the list: app-scoped, ordinary, and
the app's own save prompts still apply.

### 22. Closed menu items report a 0×0 frame at the screen corner (high — found twice)

Measured across Finder, Safari, TextEdit, Messages, Preview: a closed menu item returns a
**non-nil** zero-size frame at `(0, screen-bottom)`. So `guard let frame` passes, and the
midpoint is one pixel below the bottom-left corner. Rung 2 would post a click there whenever
`AXPress` returned non-success; rung 4 would send the *real cursor* there — the Dock, or a hot
corner, where Lock Screen is a common assignment. Two fixes, because either alone leaves a
hole: `pressShortcut` no longer lets the ladder fall past rung 1 (`AXPress` is the only
meaningful way to actuate a menu item, so `allowHardwareInput` is not even accepted there),
and zero-area frames are now treated as no frame at all on both click paths.

### 23. A timed-out request kept driving the machine (high)

The socket's 30 s timeout replied and moved on, but nothing cancelled the work. With rung 4
implemented that means the real cursor moving and keys landing for an action the caller has
already been told failed — possibly interleaved with the retry it sends next. The request is
now cancelled, and cancellation is *observed* where it matters: `try? await Task.sleep`
swallows it silently, so the hardware rung checks `Task.isCancelled` before taking the cursor
and between every character it types.

### 24. `screenshot --path` was an arbitrary-file overwrite (high)

The caller's path went straight to `CGImageDestinationCreateWithURL`, which truncates whatever
is there: `--path ~/.zshrc` replaces it with PNG bytes. Over MCP that path is written by a
*model*, so a hallucinated or mis-joined path is the likely trigger rather than an attacker.
Now: must end in `.png`, must not already exist (there is no undo for a truncated file), and
must resolve under Desktop, Downloads, Pictures, `/tmp`, or the app's own captures directory.
Captures also expire after 24 hours — screen contents are the most sensitive thing this app
produces and they were accumulating forever, which is the same argument the lease TTL makes.

### 25. Malformed numbers crashed the app (medium)

`Duration.seconds(minutes * 60)` and `Int((width * scale).rounded())` both trap on extreme or
non-finite input, and the CLI's `Double(argument)` happily parses `"inf"`. Verified: `1e30`
and `inf` trap, though **not** the `1e18` the reviewer reported — the class was real, the
figure was not. All numeric fields are now bounded at the socket boundary, and the request is
rejected rather than clamped: a caller who asked to click at infinity has a bug, and clicking
at the screen edge instead would act on a coordinate nobody chose.

### 26. The `.focused` re-fetch re-opened item 7 (high)

The re-fetch added hours earlier accepted a replacement element on a **role** match. For the
`.focused` locator the re-fetch query *is* `ElementQuery.focused` — the same query rung 2 uses
to detect text landing in the wrong field — so the guard reduced to comparing the focused
element against itself, and any same-role field that happened to hold focus would confirm text
that landed in it. That is exactly item 7, resurrected through the machinery meant to fix item
D. Matching is now on the full signature (role, label, origin, size) captured while the
element was alive: a rebuilt-in-place element still matches, a different field does not.

### 27. Unreadable was being reported as refuted (medium)

Rung 2 treated a nil read-back as "did not land", when it only means the target exposes no
value or its handle died. Two consequences: a false `noEffect` invites a retry that types the
text twice, and with hardware input allowed rung 4 *would* retype it — after which
`contains(text)` matches the doubled string and reports `confirmed`. Rung 1 already refuses to
retry for this exact reason; rung 2 now stops at `unverifiable` instead of falling through.

### 28. Two confirmation channels could confirm nothing happening (medium)

`addingSelectionEvidence` accepted a nil `before` as a baseline, so a selection read that
failed (nothing focused, a busy app, an AX timeout) followed by a successful read of a
pre-existing selection confirmed a press that did nothing. It now requires a real baseline.
And the window-level pixel diff used a threshold calibrated for a caret inside a small element
rect; applied to a whole window, any playing video or animated avatar clears it continuously,
so *every* no-op shortcut on such a window would have confirmed. A control capture now runs
before the action: if the window changes on its own while we do nothing, its pixels are not
allowed to testify about what we did.

### 28a. Any local process could kill the app by connecting and dying (critical — found while testing the fixes)

The most severe finding of the day, and no reviewer found it: it surfaced from *running* the
input-validation tests rather than reading code. Sending `--x inf` crashed the CLI mid-request
(`JSONSerialization` raises an uncatchable ObjC exception on non-finite numbers, before the
request is ever sent) — and **Rocuronium.app died with it**. No crash report, stale socket file
left behind, which is the signature of SIGPIPE rather than a trap.

Root cause: `SO_NOSIGPIPE` was set after the authorization check, while the rejection branch
writes its error *before* that point. So a write to a socket whose client had already vanished
raised SIGPIPE, whose default action terminates the process. Reproduced deterministically with
a nine-line unsigned client that connects and `SIGKILL`s itself: the app died every time.

That makes it an unauthenticated denial of service against the process holding the
Accessibility grant — reachable by *any* local process, no signature required, precisely the
class the socket's authorization is supposed to bound. The option is now set immediately after
`accept`, before any write on that descriptor can happen. The CLI separately refuses
non-finite numeric flags, since dying during serialization tells the user nothing.

Two lessons worth keeping. The authorization check was carefully designed and correct on its
own terms, but the *ordering* around it created a hole underneath it — a rejected connection
still ran code. And three reviewers reading the same diff missed what one malformed argument
found in a second, which is the argument for keeping a live machine in the loop rather than
reviewing on paper alone.

### 29. Smaller, still real

`screenshot` of a region or the full display had no display-asleep guard, so it returned
`ok: true` with a black frame — a correct-looking picture of nothing, in the overnight case
this tool is for. Rung 4 typed blind after clicking: the occlusion check ran *before* a click
that takes ~110 ms, so it now confirms the target actually holds the keyboard before typing,
and reports how far it got if the screen locks mid-run. MCP forwarded undeclared arguments
verbatim, which let a property a tool never advertised through to the router; arguments are
now filtered against each tool's own schema. `park` accepted coordinates on no display at all
(and the fix had to use `CGDisplayBounds`, not `NSScreen.frame`, or it would have rejected
valid points on every non-primary display — the coordinate trap again). And `ControlServer`
still carried a comment describing the code-signature check as a "known gap" directly above
the code that performs it; it now says what the check does and does not buy, which is
attribution rather than exclusion, since the bundle ships a signed CLI that forwards commands
for anyone able to exec it.

## Found while wiring the display and vision commands (same day, later session)

### 15. A region capture of an occluded window returned someone else's pixels (high)

Found by looking at the output rather than the return code, which is becoming the house method.
The first `screenshot --app TextEdit` implementation captured the *display region* under the
window's AX frame; TextEdit was behind Discord, so the command returned a Discord conversation
at exactly TextEdit's size — a correct-looking image of entirely the wrong thing, and one a
model consuming it would have happily described as "the app". App captures now go through
`SCContentFilter(desktopIndependentWindow:)`, which composites the window's own pixels
regardless of occlusion (and works for windows parked on the virtual display). The reply names
the window and frame actually captured, so ScreenCaptureKit's window pick is auditable.
Region (`--x/--y/--w/--h`) and full-display captures keep the old semantics on purpose: "what
is visible there" is the right question for a pixel diff.

### 16. Background menu-item presses: AppKit no-ops with a success code, Electron works

Measured while building `shortcut` (keyboard shortcuts via `AXPress` on the menu item carrying
them, the Codex-derived technique from item D). Both halves of the assumption needed testing,
and they came out opposite:

- **AppKit (TextEdit), app in background**: the menu item reports `AXEnabled = false`,
  `AXPress` returns `.success`, and nothing happens — verified with the selection collapsed
  first (`AXSelectedTextRange` written and read back as `loc 0 len 0`), pressed, and polled to
  3 s: the selection never changed. Inactive AppKit apps never validate their menus, and the
  press of an unvalidated item is discarded *with a success code* — one more entry in the
  return-codes-lie ledger. An earlier run that looked like a working background press was a
  stale full selection from a frontmost test; the collapse that "preceded" it had silently
  targeted the wrong window. Frontmost, the same press works.
- **Electron (Postman), app in background, window parked on the virtual display**: menu items
  stay enabled (Electron sets enablement explicitly, no AppKit auto-validation), and the
  press is **unreliable**. `File ▸ New Tab` pressed shortly after launch opened a real tab —
  verified by window-true screenshot, no cursor, no focus change, no visible pixels. But
  `File ▸ Close Tab` no-oped, and a second `New Tab` minutes later no-oped too, all with
  success codes. Plausible mechanisms — handlers keyed on a null `getFocusedWindow()`, App
  Nap — are app-specific and were not chased; the operative fact is that the same item on the
  same app can work and then silently stop, and nothing in the return path says which
  happened. The recorded `pixelDelta: 0` on the no-ops was accurate, not a capture artifact.

Consequences shipped: the `AXEnabled` report does not gate the press (it is recorded and the
reply's note explains the AppKit case); a changed `AXSelectedText` on the focused element is a
confirmation channel (it can only confirm — an unchanged selection proves nothing, and Select
All on an already-full selection is legitimately idempotent); window-true pixel diffs may only
confirm a shortcut, never refute it, because copy and its siblings succeed while changing
nothing visible; and the measured `pixelDelta` is recorded in the evidence even when it decides
nothing, because a vanished measurement reads as a mystery instead of a number.

### 17. A hardware click would have hit whatever was on top (high — self-review)

Found while reviewing the rung-4 code written the same hour. Every other rung uses
`postToPid`, which delivers to a *process* through any amount of occlusion — so the ladder's
whole design habit is "aim at the element, the process gets it". A real HID click breaks that
assumption silently: it goes to whatever window is **topmost at the coordinate**. Measured
here: fifteen windows overlapped one test point on the main display, with Discord on top. An
agent aiming at an occluded target would have taken the user's cursor to click a different
app entirely, and the evidence would have described the intended target.

Fixed by checking the frontmost *ordinary* window at the aim point (layer 0, so the Dock's
full-screen backing window is not mistaken for an occluder) and refusing when it belongs to
another process, naming the occluder. Verified both branches: a crowded main-display point
resolves to Discord (refuse), a window parked on the virtual display resolves to the target
(proceed) — which makes parking the reliable way to satisfy the guard, and ties rung 4 to the
isolation machinery rather than leaving it a free-floating hazard.

The general lesson is worth more than the fix: **when a new rung changes the delivery
mechanism, every assumption the other rungs share has to be re-derived, not inherited.**

### 18. `park` would strand a window on a display nobody was holding (medium — self-review)

Found by running the error paths and getting a *success* where a refusal belonged: `park --app
Finder` with no lease outstanding moved a real Finder window onto the virtual screen, because
the check was "does a virtual screen exist" rather than "is anyone keeping it alive". The
display in that moment was the user's own, running with zero leases.

The failure it invites: park a window, then the display goes — our teardown, a lease expiry,
or the user quitting a display they started — and the window is stranded somewhere its owner
cannot see or reach. Parking onto the virtual screen now requires holding a lease; an explicit
`--x/--y` move is still unrestricted, since that is just a window move and the reply carries
the previous position as its undo.

(The window was put back at its exact original coordinates, verified by re-reading the frame.)

### 19. The `noEffect` visual check never ran (high — self-review)

Item 11 above records that the ladder's fall-through paths asserted `noEffect` while holding
an unused capture, and states the visual check "now applies". It did not. All three call sites
pass `.noEffect` into `finish`, then call `addingVisualEvidence` — whose guard admitted only
`.unverifiable`. So those paths captured a baseline, computed a diff, and discarded the
answer, in exactly the case where pixels are the only signal left. The call was added; the
guard made it dead on arrival.

Only re-deriving the path found it — reading the note would not have, which is the argument
for treating one's own review doc as a claim rather than a record. `.noEffect` now
participates asymmetrically: a delta above the significant threshold overturns it, a mid-band
flicker never softens it to `unverifiable` (read-back that refuted the action outranks a caret
blink), and the measurement is recorded either way.

The practical rule that falls out: **menu-press shortcuts are dependable on the frontmost app
and best-effort everywhere else** — and the verdict system already says which one happened.
Every no-op in this section reported `ok: false, verdict: unverifiable`; nothing false was
ever confirmed. Also observed in passing: virtual-display leases are in-memory and die with
the app (an install cycle restarted it mid-lease) — consistent with the expiry philosophy,
and the user-started display was correctly left untouched.
