.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.moved-aside
.swiftpm/
.build/
# Docs/ is a local working area — architecture notes, roadmap, and away-experiment logs that
# contain real session window titles and browsing history. It stays on disk, never in the repo.
Docs/

# Agent worktrees are their own checkouts, never content of this one.
.claude/worktrees/
.detector-training/

# Design prototypes are local scratch, never shipped.
Prototypes/
